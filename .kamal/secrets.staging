# This file is safe to commit: it references generated local secrets and
# does not contain raw credentials.

RAILS_MASTER_KEY=$(bin/kamal-secret staging RAILS_MASTER_KEY)
DATABASE_PASSWORD=$(bin/kamal-secret staging DATABASE_PASSWORD)
POSTGRES_PASSWORD=$(bin/kamal-secret staging POSTGRES_PASSWORD)
ENCRYPTION_KEY=$(bin/kamal-secret staging ENCRYPTION_KEY)
SECRET_KEY_BASE=$(bin/kamal-secret staging SECRET_KEY_BASE)
